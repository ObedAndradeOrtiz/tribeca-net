---
deployment:
  tasks:
    - export DEPLOYPATH=/home/guislain/tribeca.guislaincorp.com
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/rsync -a --delete --exclude='.git' --exclude='.env' ./ $DEPLOYPATH/
